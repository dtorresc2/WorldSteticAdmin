import { MovimientosPipe } from './movimientos.pipe';

describe('MovimientosPipe', () => {
  it('create an instance', () => {
    const pipe = new MovimientosPipe();
    expect(pipe).toBeTruthy();
  });
});
